<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Planify Email Verification</title>
    <style>
        /* General resets */
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #f4f6f8;
            margin: 0;
            padding: 0;
            color: #333333;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            border-top: 5px solid #4f46e5;
        }

        .header {
            background-color: #4f46e5;
            color: white;
            padding: 30px;
            text-align: center;
            font-size: 28px;
            font-weight: bold;
        }

        .content {
            padding: 30px;
            line-height: 1.6;
        }

        .content p {
            margin-bottom: 20px;
        }

        .verify-button {
            display: inline-block;
            background-color: #4f46e5;
            color: white !important;
            text-decoration: none;
            padding: 15px 25px;
            border-radius: 8px;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .verify-button:hover {
            background-color: #3730a3;
        }

        .footer {
            background-color: #f4f6f8;
            color: #888888;
            text-align: center;
            padding: 20px;
            font-size: 12px;
        }

        .company-name {
            font-weight: bold;
            color: #4f46e5;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">Planify</div>
        <div class="content">
            <p>Hi <%= name %>,</p>
            <p>Welcome to <span class="company-name">Planify</span>! Please verify your email address to get started.
            </p>
            <p style="text-align:center;">
                <a class="verify-button" href="<%= verificationLink %>">Verify Email</a>
            </p>
            <!-- <p>If the button doesn't work, copy and paste the following link into your browser:</p>
            <p style="word-break: break-all;">
                <%= verificationLink %>
            </p> -->
        </div>
        <div class="footer">
            &copy; <%= new Date().getFullYear() %> Planify. All rights reserved.
        </div>
    </div>
</body>

</html>